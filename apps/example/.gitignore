node_modules/

# Continuous Native Generation: ios/ and android/ are regenerated by
# `expo prebuild` from app.config.js + the react-native-automotive-expo-plugin.
# Treating them as build artefacts (not source) lets us verify the plugin
# really produces correct native config on every CI run.
ios/
android/

# Expo dev artefacts
.expo/
.expo-shared/
expo-env.d.ts

# Metro
.metro-health-check*

# Build
*.tsbuildinfo
*.log
*.jsbundle
