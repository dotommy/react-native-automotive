# Sample workflow for building and deploying a Jekyll site to GitHub Pages
name: ci/gh-pages

# Disabled: this workflow deploys apps/docs (Next.js + Nextra) which is
# scheduled for replacement with VitePress in Step 11 of the BRIEF
# roadmap. It also uses actions/upload-pages-artifact@v2 which transitively
# depends on the deprecated actions/upload-artifact@v3, blocking execution.
# Re-enable / replace with VitePress build pipeline when migration lands.
on:
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Use Node.js 16
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn config get cacheFolder)"
      - name: Restore yarn cache
        uses: actions/cache@v3
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: yarn-cache-folder-${{ hashFiles('**/yarn.lock', '.yarnrc.yml') }}
          restore-keys: |
            yarn-cache-folder-
      - name: Install dependencies
        run: |
          yarn install --immutable
      - name: Build
        run: yarn workspace @carplay/docs build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./apps/docs/out

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
